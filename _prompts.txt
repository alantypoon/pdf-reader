add a button "AI Generation" of generating revision flash cards and MCQ quiz questions that helps the user to learn about page by aigateway for ai inference. use a right-side drawer to display the flash card and mcq quiz. 

- use the following aigateway settings in .env

AIGATEWAY_API_URL=https://aigateway.aied.hku.hk/api/generate
AIGATEWAY_PROVIDER=ett
AIGATEWAY_MODEL=vllm|OpenGVLab/InternVL3_5-38B
AIGATEWAY_APIKEY=<secretly hidden>

- use the following sample code for ai inference calling:
import { openAsBlob } from 'node:fs';

const endpointUrl = "https://aigateway.aied.hku.hk/api/generate?debug_log=1";

async function runEttExtraction() {
  const formData = new FormData();
  formData.append('provider', 'ett');
  formData.append('apiKey', "<secretly hidden>");
  formData.append('model', "vllm|OpenGVLab/InternVL3_5-38B");
  formData.append('links', JSON.stringify(['https://example.com/article-one', 'https://example.com/article-two']));
  formData.append('wordCount', '1000');
  formData.append('files', await openAsBlob('./brief.pdf'), 'brief.pdf');
  formData.append('files', await openAsBlob('./screenshot.png'), 'screenshot.png');

  const response = await fetch(endpointUrl, {
    method: 'POST',
    headers: { Accept: "text/event-stream" },
    body: formData,
  });

  console.log("HTTP", response.status);
  console.log(await response.text());
}
runEttExtraction().catch(console.error);

- save the user generated flash cards and quizs to database by MONGODB_URI in .env


////

in backend server, store each user action in the database collection user-actions, i.e. login, logout, reading which page, spend how long before turning to next page, use of ai generation, answering quiz questions, etc.


////

before each ai generation, must check if there exists a generated document in the database pdf-reader in the collection ai-generations. if there is one, load from the database instead of generating a new one, unless regenerate button is clicked, it will regenerate the document. do not allow mutliple versions of documents for the same page. remove the previous one before writing the new one.

when regenerate button is clicked, ask for user confirmation before proceeding.

///

why the book 1a, section 1, page 1, 1-1.png always starts new ai generation when the ai generation button is clicked? there should be book/section/page equal to 1a/1/1 exists so that no new ai generation is required. just load the document from the database and render the drawer page to the user is fine. but why the generation will be taken place every time?

///

when contents.json contains /name, append it to the book's label text, e.g. E1 - Microbes and Disease while the value is still e1.

use upper case for the book's ID, for example 1A, 1B, etc. while the value is still using the lower case.

///

1. when zooming, the text should be changed proportionally.

2. when color button is clicked, show a dialog with the most frequently used 12 colors for pen and highlighter. Next to them, there is a customized button to open an extended panel to choose any color.


///

rename erase all to erase book and ask for sweetalert2 confirmation before proceeding

///

instead of using the cols slider, can we use the same percentage zooming slider on the button panel to control the size of thumbnails?

////

should i remove the hythen character - in book and section on physics and biology to save space?

///

add keystroke navigation: always allow left and right arrows key to turn to previous and next page. ctrl left and right to previous and next section, shift left and right to previous and next book. it should allow even when ai generation drawer is open.

///

advise how to implement the search function, it should be able to do it:
- on all/one subject, book, section, page
- where should the search field be placed?
- how to handle the various cases above?
- should find the filter value in the database collection ai-generations summary field value
- should the results be shown inline, modal or drawer? 

///

why sometimes rubber tool cannot remove a stroke?


///

when color button is clicked, show a dialog with the most frequently and popularly used 12 colors for pen and highlighter. Next to them, add a customized button to open another panel to choose from any color (the current one).

///

when raymond logs in, the user id will be shown as "up3g29ozb" instead of "raymond", why?

///

when using two fingers to scroll the screen on my iphone or ipad,
the scrolling speed seems to be doubled.
when scrolling on desktop by mouse, the speed is normal.
are there two event handling processes in the code that makes the mistake?
or i should reduce the scrolling speed in a half?

///

when the buttons on the side bar is sufficient to be displayed, do not split them into two columns.

////

when fullscreen toggle is clicked or the screen is resized or sidebar is toggled or bottombar is toggled, refresh the layout including the sidebar buttons, the bottombar buttons and central content area. it must always show all the buttons and necessary elements i.e. close buttons, fullscreen toggle, sidebar and bottombar (when they are not closed) 

////
/// leave color button as a separate button on the bottombar.

totally hide the zooming percentage controls
add icons on the highlighter, pen, text, rubber, move, color.

i.e. - 100% +. remove the annotation TOGGLE button, show the grouped buttons as mentioned before: it groups all the tool buttons (highlighter, pen, text, rubber, move) into one single button on the bottombar with a right button with a upward smaller button that will open a pullup menu to select the tools instead. default it is highlighter tool. keep the menu in this order: highlighter, pen, text, rubber, move, color.

////

